{
  "mcpServers": {
    "scrapegraph": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/ScrapeGraphAI/scrapegraph-mcp",
        "scrapegraph-mcp"
      ],
      "env": {
        "SGAI_API_KEY": "${SGAI_API_KEY}"
      }
    }
  }
}
